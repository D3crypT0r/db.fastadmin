# db.fastadmin

gem install bundler
bundle install


# Cross-Platform Installation Script
curl -sSL https://get.rvm.io | bash -s stable --ruby
source ~/.rvm/scripts/rvm
gem install bundler

# Ubuntu/Debian: 
Use sudo apt update && sudo apt install -y ruby-full && gem install bundler.
# macOS: 
Use brew install ruby && gem install bundler.
# Windows:
Install RubyInstaller and then run gem install bundler

> ruby exploit.rb -u
> ruby exploit.rb -f urls.txt

